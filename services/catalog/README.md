# Catalog Service

Minimal ShopCloud catalog microservice.

## Endpoints

- `GET /health`
- `GET /products`
- `GET /products?category=electronics`
- `GET /products/:id`

## Run locally

```bash
npm install
npm start
```

Default port: `3001`
