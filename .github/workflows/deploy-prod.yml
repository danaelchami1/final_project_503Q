name: Deploy Prod

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy to production (commit SHA or release tag)"
        required: true
        type: string

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to production environment
        run: |
          echo "TODO: deploy image tag ${{ inputs.image_tag }} to prod target (EKS/ECS/Kubernetes manifests)"

      - name: Verify post-deployment health
        run: |
          echo "TODO: run production smoke/health verification"
