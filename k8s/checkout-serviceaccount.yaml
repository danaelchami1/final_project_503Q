apiVersion: v1
kind: ServiceAccount
metadata:
  name: checkout-sa
  annotations:
    # Replace with terraform output checkout_irsa_role_arn when enabled.
    eks.amazonaws.com/role-arn: "REPLACE_WITH_CHECKOUT_IRSA_ROLE_ARN"
