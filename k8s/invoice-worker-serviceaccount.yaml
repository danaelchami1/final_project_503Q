apiVersion: v1
kind: ServiceAccount
metadata:
  name: invoice-worker-sa
  annotations:
    # Replace with Terraform output invoice_worker_irsa_role_arn.
    eks.amazonaws.com/role-arn: "REPLACE_WITH_INVOICE_WORKER_IRSA_ROLE_ARN"
